# src/Makevars — portable build flags for ply package

# Rcpp include path is injected automatically via LinkingTo: Rcpp in DESCRIPTION

CXX_STD = CXX17
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS)
